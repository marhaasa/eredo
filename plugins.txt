# Claude Code plugins to install on first start, one per line, e.g.
#   context7@claude-plugins-official
# Override with ~/.config/eredo/plugins.txt. Plugins that talk to
# remote MCP servers need their domains in the allowlist.
#
# A plugin from another marketplace needs that marketplace registered first:
#   # marketplace: owner/repo
